-include ../arch.mk
-include ../../arch.mk
-include ../../../arch.mk
-include ../../../../arch.mk
-include ../../../../../arch.mk


# Path to install the executibles.
DESTDIR = ${COMRISB_BIN}

# (mpi) fortran compiler
F90 = ${PF90}
F77 = ${PF90}

# Compiler options
FFLAGS = ${compfl}

# Math kernal library
MKL_LIB = ${LAPACK_LIB}

# mpi version
Fmpi_define = -DMPI -Dmpi_mode
